import { useNavigate } from 'react-router-dom';import { ArrowLeft } from 'lucide-react';import { useAcademicWorld } from '@/hooks/useAcademicWorld';import { InteractiveCareerTree } from '@/components/curriculum/InteractiveCareerTree';
export default function Curriculum(){const {world,updateCourse}=useAcademicWorld();const nav=useNavigate();return <main className="page-shell"><button className="back" onClick={()=>nav('/dashboard')}><ArrowLeft/> Volver al Reino</button><p className="eyebrow">Mapa de progresión</p><h1>Tu malla es el camino.</h1><p className="lead">Actualiza un curso y los caminos dependientes se despejarán automáticamente.</p><InteractiveCareerTree courses={world.courses} onUpdate={updateCourse}/></main>}
