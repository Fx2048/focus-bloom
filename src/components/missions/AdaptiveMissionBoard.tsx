import { Mission } from '@/types/academicWorld'; import { Play, Moon, Flame } from 'lucide-react';
export function AdaptiveMissionBoard({missions,onStart}:{missions:Mission[];onStart:(m:Mission)=>void}){return <aside className="mission-board"><div className="flex items-center justify-between"><div><p className="eyebrow">Plan adaptativo</p><h2>Misiones de hoy</h2></div><span className="rounded-lg bg-white/10 p-2"><Flame size={18}/></span></div><div className="mt-4 space-y-3">{missions.map(m=><article key={m.id} className={`mission mission-${m.priority}`}><div className="flex justify-between gap-2"><div><p className="font-extrabold">{m.priority==='recovery'&&<Moon size={13} className="mr-1 inline"/>}{m.title}</p><p className="mt-1 text-xs opacity-75">{m.description}</p></div><span className="text-xs font-bold">+{m.xp} XP</span></div><button onClick={()=>onStart(m)}><Play size={14} fill="currentColor"/> Comenzar · {m.minutes} min</button></article>)}</div></aside>}
